import { Phone, MessageCircle, MapPin } from "lucide-react";
import { Link } from "react-router-dom";
import { business } from "../data/business";
import { services } from "../data/services";
import { useLanguage } from "../context/LanguageContext";
import "./Footer.css";
export default function Footer() { const { lang, t } = useLanguage(); return <footer className="footer"><div className="container footer-grid"><div className="footer-brand"><div className="footer-logo">श्री</div><h3>{lang === "hi" ? business.nameHi : business.nameEn}</h3><p>{lang === "hi" ? `मालिक: ${business.ownerHi}` : `Owner: ${business.ownerHi}`}<br />{t.common.bikaner}</p></div><div><h4>{t.footer.quick}</h4><div className="footer-links">{[["/", t.nav.home], ["/services", t.nav.services], ["/fleet", t.nav.fleet], ["/tours", t.nav.tours], ["/wedding", t.nav.wedding], ["/about", t.nav.about], ["/contact", t.nav.contact]].map(([href, label]) => <Link key={href} to={href}>{label}</Link>)}</div></div><div><h4>{t.footer.services}</h4><div className="footer-links">{services.slice(0, 5).map(service => <Link key={service.id} to="/services">{lang === "hi" ? service.titleHi : service.titleEn}</Link>)}</div></div><div><h4>{t.footer.contact}</h4><div className="footer-contact"><div><MapPin size={17} /><span>{t.common.bikaner}</span></div><a href={`tel:${business.phoneTel}`}><Phone size={17} /><span>{business.phoneDisplay}</span></a><a href={business.whatsappBase} target="_blank" rel="noopener noreferrer"><MessageCircle size={17} /><span>WhatsApp</span></a></div></div></div><div className="footer-bottom"><div className="container">© {new Date().getFullYear()} {business.nameEn}. {t.footer.rights}</div></div></footer>; }
